'use strict';

// import the LinkedList file
const LinkedList = require('../linked-list.js');

// generic test
describe('Linked List', () => {
  it('works', () => {
    expect(true).toBeTruthy();
  });
});

// test to see if toString returns/logs anything

// test to see if we can change the head of a linkedList with `insert`

// test to see if we can create new `Node`s without specifying a `next` property

// test to see if the `included` method returns true or false
