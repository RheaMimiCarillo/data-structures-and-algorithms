'use strict';

const Graph = require('../Graph');
const Queue = require('../stack-and-queue/queue');

describe('Testing the Breadth-first traversal', () =>
{
  test('Graph without any nodes', () =>
  {
    //
  });
  test('Graph without any connected nodes', () =>
  {
    //
  });
  test('Graph where all nodes can be visited', () =>
  {
    //
  });
});
