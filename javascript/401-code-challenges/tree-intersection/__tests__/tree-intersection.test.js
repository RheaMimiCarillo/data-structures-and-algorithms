'use strict';
const HashTable = require('../HashTable');

// import BinaryTree class

describe('Testing tree-intersection', () =>
{
  test('Should return "No matching values found" for empty trees', () =>
  {
    let testHashTable = new HashTable(1024);

    expect();
  });
  test('Should return "No repeated words found" for trees without any matching values', () =>
  {
    let testHashTable = new HashTable(1024);

    expect();
  });
  test('Should return an array of values found in both trees', () =>
  {
    let testHashTable = new HashTable(1024);

    expect();
  });
});
